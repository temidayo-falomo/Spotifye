import styled from "styled-components";

export const StyledLyrics = styled.article`
  width: 100%;
  overflow-y: auto;
  max-height: 550px;

  article {
    word-wrap: break-word;
    white-space: pre-wrap;
    font-size: 1.5rem;
    line-height: 1.8em;
  }
`;
